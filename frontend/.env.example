# Base URL of the FastAPI backend.
#
# Leave UNSET in development to use Vite's /api proxy (see vite.config.js),
# which avoids any CORS configuration on the FastAPI side.
# Set it explicitly for a production build or a non-local backend.
VITE_API_BASE_URL=http://localhost:8000
