# Phase 0 dependencies only -- scoped to what the foundation actually
# needs (health-check API, dashboard skeleton, sample data, smoke
# tests, and Nidhi's approved RAG smoke test). See docs/decisions.md.
#
# Additional packages (scikit-learn, xgboost, shap, lime, fairlearn,
# langchain, etc.) will be added in the PRs that introduce Phase 1
# real logic, not pinned prematurely here.

fastapi
uvicorn
streamlit
pandas
numpy
pytest
httpx
requests
chromadb
scikit-learn
shap
lime
joblib
pydantic
groq==1.7.0
xgboost
# PDF in, PDF out -- two unrelated needs that happen to share a file format.
#
# READS the RBI source PDFs in data/rbi_sources/. Chosen over pdfplumber and
# PyMuPDF because it is pure Python (no system binaries), permissively
# licensed, and preserves the line breaks that RBI clause numbering sits on.
pypdf

# WRITE the downloadable assurance report (app/report/pdf_report.py).
reportlab
matplotlib
